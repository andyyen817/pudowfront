# 隐私页语言层级优化

记录时间：2026-05-26 15:28  
请求来源：前端 app、管理后台、后端、数据库  
本次状态：已完成隐私页二次优化、本地构建通过、本地 `/privacy` 路由返回 200，待提交推送触发 Zeabur 部署。

## 一、本次完成的任务重点

1. 优化隐私页信息层级：
   - 首屏不再把个人开发者和包名放在最前面。
   - 首屏改为公司产品角度：`AirKOP Product Privacy Policy` / `AirKOP 产品隐私政策` / `Kebijakan Privasi Produk AirKOP`。
   - App 名称、包名、开发者、更新时间移到页面后半段的 Google Play 应用资料区。
2. 优化语言切换：
   - 页面不再同时固定显示英文、中文、印尼文三段。
   - 现在会读取现有 `i18n.language`。
   - 选择英文只显示英文政策。
   - 选择中文只显示中文政策。
   - 选择印尼文只显示印尼文政策。
3. 保留 Google Play 审核需要的信息：
   - `ATMWATER`
   - `com.atmwater.client`
   - `andy颜秉田`
   - `May 26, 2026` / `2026年5月26日` / `26 Mei 2026`

## 二、本地验证结果

1. 已执行源码关键内容检查：
   - `useTranslation` 存在
   - `getContent(i18n.language)` 存在
   - 英文、中文、印尼文三套内容存在
   - `ATMWATER`、`com.atmwater.client`、`andy颜秉田` 存在
2. 已确认旧的固定三语言区块已移除：
   - 不再使用 `chineseSections`
   - 不再使用 `indonesiaSections`
   - 页面统一通过 `policy.sections.map` 渲染当前语言内容
3. 已执行生产构建：
   - 命令：`npm run build`
   - 结果：成功
4. 已执行本地 preview 路由测试：
   - 地址：`http://127.0.0.1:8080/privacy`
   - HTTP 结果：`200 OK`
   - 测试后已停止本地 preview 进程

## 三、本次代码变更范围

主要修改：

- `src/pages/Privacy.jsx`

新增沟通记录：

- `工作交流/202605261528隐私页语言层级优化前端app管理后台后端数据库.md`

## 四、前端 app 下一步

1. App 内隐私政策 URL 继续使用：
   - `https://www.airkop.com/privacy`
2. App 内语言选择若与官网语言不同步，需要前端 app 自己确认是否要传参或只依赖官网语言切换。
3. Google Play 审核关注的 App 资料仍在页面后半段，用户和审核员向下滚动即可看到。
4. App 权限声明仍需与隐私政策保持一致：
   - Camera
   - Photos/Media
   - Location
5. 若某项权限实际没有使用，应从 App 代码和 Play Console 权限说明中移除。

## 五、管理后台下一步

1. 管理后台若也有隐私政策链接，应统一指向 `https://www.airkop.com/privacy`。
2. 后台文案如需引用产品主体，建议优先使用 AirKOP 产品语境，App 资料作为合规说明。
3. 后台涉及巡检照片、KUD 审核、设备资料、账号资料的页面，应与隐私政策中的用途一致。
4. 若后台能导出、删除、查看敏感资料，需要保留操作日志和权限控制。

## 六、后端服务器下一步

1. 本次仍只修改前端官网隐私页，没有修改后端 API。
2. 后端需要继续确认隐私政策中写到的数据类型与真实服务一致：
   - 账号与联系方式
   - 设备资料
   - 巡检或维护照片
   - KUD 审核图片
   - 位置资料
   - 服务记录
3. 后端需要确认账号删除、数据访问、资料更正是否有真实处理流程。
4. 若 Google Play 后续检查删除账号功能，当前删除账号页仍需要真实后端接收渠道支持。

## 七、数据库下一步

1. 数据库需确认保存字段与隐私政策一致，避免政策声明和实际数据处理不一致。
2. 明确保留周期：
   - 账号资料
   - 巡检照片
   - 设备日志
   - KUD 审核资料
   - 位置资料
3. 明确删除请求后的处理时间、备份保留周期、软删除/硬删除规则。
4. 对手机号、位置、图片 URL、设备日志等敏感信息做好权限控制和必要脱敏。

## 八、部署与测试下一步

1. 提交本次修改到 Git。
2. 推送到 GitHub `main`。
3. Zeabur 若绑定 GitHub 自动部署，推送后应自动触发部署。
4. 部署完成后测试：
   - 打开 `https://www.airkop.com/privacy`
   - 切换英文、中文、印尼文
   - 确认每次只显示当前语言内容
   - 确认 App 资料在后半段仍可看到 `ATMWATER`、`com.atmwater.client`、`andy颜秉田`

