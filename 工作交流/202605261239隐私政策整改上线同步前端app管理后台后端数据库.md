# 隐私政策整改上线同步

记录时间：2026-05-26 12:39  
请求来源：前端 app、管理后台、后端、数据库  
本次状态：已完成前端隐私政策页面开发，已完成本地构建与本地 preview 路由检查，待提交推送触发 Zeabur 部署。

## 一、本次完成的任务重点

1. 已重写 `src/pages/Privacy.jsx`，保留现有 `/privacy` 路由，不新增独立 HTML 页面。
2. `/privacy` 页面首屏已明确显示 Google Play 审核需要识别的关键信息：
   - App name：`ATMWATER`
   - Package name：`com.atmwater.client`
   - Developer：`andy颜秉田`
   - Last updated：`May 26, 2026`
3. 隐私政策正文已补齐英文、中文、印尼语内容。
4. 英文正文已覆盖：
   - 信息收集
   - 相机、照片/媒体、位置权限用途
   - 智能水机设备资料
   - 巡检、维护、KUD 照片审核
   - 数据用途
   - 数据共享与保存
   - 数据保护
   - 联系方式
5. 联系方式已明确：
   - Email：`contact@airkop.com`
   - Phone：`+62 857 8856 8888`
   - Address：`GRHA INDUK-KUD, JL. BUNCIT RAYA NO. 20, Pejaten Barat, Pasar Minggu, Jakarta Selatan, DKI Jakarta, 12510`

## 二、本地验证结果

1. 已执行 UTF-8 内容检查，确认源码真实包含：
   - `andy颜秉田`
   - `ATMWATER`
   - `com.atmwater.client`
   - `隐私政策`
2. 已执行依赖安装：
   - 命令：`npm install`
   - 结果：成功
   - 注意：npm audit 显示 9 个依赖漏洞告警（5 moderate、4 high），本次未执行自动修复，避免破坏版本兼容。
3. 已执行生产构建：
   - 命令：`npm run build`
   - 结果：成功
4. 已启动本地 preview 测试：
   - 本地地址：`http://127.0.0.1:8080/`
   - 路由检查：`http://127.0.0.1:8080/privacy`
   - HTTP 结果：`200 OK`
   - 测试后已停止本地 preview 进程。

## 三、本次代码变更范围

主要修改：

- `src/pages/Privacy.jsx`

新增沟通记录：

- `工作交流/202605261239隐私政策整改上线同步前端app管理后台后端数据库.md`

依赖影响：

- 执行 `npm install` 后，本地生成了 `node_modules`，但该目录应由 `.gitignore` 忽略，不应提交。
- `package-lock.json` 可能因 npm 版本或依赖解析产生差异，提交前需要确认是否有实际变更。

## 四、前端 app 下一步

1. App 内隐私政策 URL 保持或更新为：
   - `https://www.airkop.com/privacy`
2. Google Play Console 中 App 名称、包名、开发者名称需要与页面一致：
   - `ATMWATER`
   - `com.atmwater.client`
   - `andy颜秉田`
3. App 权限声明需要与隐私政策一致：
   - Camera：设备二维码扫描、巡检/维护照片
   - Photos/Media：上传巡检、维护、KUD 审核图片
   - Location：发现、连接、巡检或维护附近智能水机设备
4. 如果 App 实际没有使用某项权限，需要从 App 权限声明和代码中移除，不要只靠隐私政策解释。
5. Zeabur 部署完成后，用手机浏览器和桌面浏览器打开 `https://www.airkop.com/privacy`，确认首屏能看到 ATMWATER、包名和开发者名称。

## 五、管理后台下一步

1. 若管理后台会查看巡检照片、维护记录、设备告警、KUD 审核图片，需要确保后台显示的数据用途与隐私政策一致。
2. 后台需要确认是否有操作日志，尤其是查看、审核、删除、导出用户或巡检资料的动作。
3. 后台如果处理账号删除或资料更正请求，需要与 `contact@airkop.com` 或后端工单流程同步。
4. 后台文案中如出现 App 名称，建议统一使用 `ATMWATER`。

## 六、后端服务器下一步

1. 本前端项目未包含真实后端 API，本次只完成官网隐私政策整改。
2. 后端需要确认真实保存的数据与隐私政策一致，包括：
   - 用户资料
   - 设备资料
   - 巡检/维护照片
   - KUD 审核图片
   - 位置资料
   - 服务或交易记录
3. 后端需要确认图片上传与资料访问使用 HTTPS，并有权限控制。
4. 后端需要提供真实的账号删除、资料更正、数据访问请求处理流程。
5. 若 Google Play 后续审核删除账号功能，当前网站 `DeleteAccount.jsx` 仍是前端模拟提交，需要后端支持真实提交渠道。

## 七、数据库下一步

1. 数据库需要确认隐私政策中提到的数据是否真实存在，以及字段位置。
2. 需要明确资料保留周期：
   - 账号资料保留多久
   - 巡检/维护记录保留多久
   - 图片、位置、设备日志保留多久
   - 删除请求后多久完成处理
3. 需要明确是否有备份数据、软删除数据、审计日志，以及这些数据的保留周期。
4. 建议对手机号、位置、敏感图片 URL 或访问凭证做权限控制和必要脱敏。

## 八、部署与测试下一步

1. 提交本次修改到 Git。
2. 推送到 GitHub `main` 分支。
3. Zeabur 若已绑定 GitHub 自动部署，推送后应自动触发部署。
4. 部署完成后访问：
   - `https://www.airkop.com/privacy`
5. 检查首屏必须能看到：
   - `Privacy Policy for ATMWATER`
   - `com.atmwater.client`
   - `andy颜秉田`
6. 检查完成后再回 Google Play Console 重新提交审核。

